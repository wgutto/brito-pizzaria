"use client"

type Props = {
    onValidade: (hasEmail: boolean, email: string) => void
}

export const LoginAreaStepEmail = ({ onValidade }: Props) => {
    return (
        <div>
            
        </div>
    )
}